@{
    ViewData["Title"] = "Comments & Evidence";
}

<div class="card shadow-sm">
    <div class="card-header">Evidence & Comments</div>
    <div class="card-body">
        <div class="mb-3">
            <label class="form-label">Add Comment</label>
            <textarea class="form-control" rows="3"></textarea>
            <button class="btn btn-primary mt-2">Post</button>
        </div>
        <div class="border-top pt-3">
            <div class="fw-bold">Reviewer Note</div>
            <p>Attach payroll approval memo.</p>
        </div>
    </div>
</div>
